$(document).ready(function () {

    //$('body').on('focusin focusout', '.twr-form-control', function (e) {
    //    var input = $(this);
    //    if (e.type === 'focusin') {
    //        input.addClass('twr-focus');
    //    } else {
    //        input.removeClass('twr-focus');
    //        if (input.val().length > 0) {
    //            input.addClass('twr-has-value');
    //        } else {
    //            input.removeClass('twr-has-value');
    //        }
    //    }
    //});
});